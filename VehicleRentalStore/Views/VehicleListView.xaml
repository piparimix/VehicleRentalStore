<UserControl
    x:Class="VehicleRentalStore.Views.VehicleListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:VehicleRentalStore.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:VehicleRentalStore.ViewModels"
    d:DesignHeight="600"
    d:DesignWidth="1000"
    mc:Ignorable="d">

    <Grid Margin="20" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="24"
                FontWeight="Bold"
                Foreground="{DynamicResource TextHighBrush}"
                Text="Vehicles" />

            <Border
                Grid.Column="1"
                Height="38"
                Margin="0,0,15,0"
                Background="{DynamicResource ControlBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        Margin="12,0,8,0"
                        VerticalAlignment="Center"
                        FontFamily="Segoe MDL2 Assets"
                        Foreground="{DynamicResource TextMutedBrush}"
                        Text="&#xE721;" />

                    <TextBox
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextMutedBrush}"
                        Text="Search vehicles..." />
                </Grid>
            </Border>

            <Button
                Grid.Column="2"
                Height="38"
                Padding="20,0"
                Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                CommandParameter="{x:Type viewmodels:RegisterVehicleViewModel}"
                Style="{StaticResource SuccessButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        FontFamily="Segoe MDL2 Assets"
                        Text="&#xE710;" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontWeight="Bold"
                        Text="New Vehicle" />
                </StackPanel>
            </Button>
        </Grid>

        <Border
            Grid.Row="1"
            Margin="0"
            Style="{StaticResource BorderStyle}">
            <DataGrid
                AutoGenerateColumns="False"
                BorderThickness="0"
                IsReadOnly="True"
                ItemsSource="{Binding Fleet}"
                RowDetailsVisibilityMode="VisibleWhenSelected"
                Style="{StaticResource {x:Type DataGrid}}">

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="1.5*" Header="Vehicle Identity">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock
                                        FontWeight="Bold"
                                        Foreground="{DynamicResource TextHighBrush}"
                                        Text="{Binding Brand}" />
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="{DynamicResource TextMutedBrush}"
                                        Text="{Binding Model}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Width="120" Header="Type">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border
                                    Padding="8,3"
                                    HorizontalAlignment="Left"
                                    Background="{DynamicResource ControlBackgroundBrush}"
                                    CornerRadius="4">
                                    <TextBlock
                                        FontSize="12"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextMutedBrush}"
                                        Text="{Binding VehicleType}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn
                        Width="80"
                        Binding="{Binding ManufactureYear}"
                        Header="Year" />

                    <DataGridTemplateColumn Width="120" Header="License Plate">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border
                                    Padding="6,2"
                                    HorizontalAlignment="Left"
                                    Background="{DynamicResource ControlBackgroundBrush}"
                                    CornerRadius="3">
                                    <TextBlock
                                        FontFamily="Consolas"
                                        FontWeight="SemiBold"
                                        Text="{Binding LicensePlate}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn
                        Width="110"
                        Binding="{Binding Status}"
                        Header="Status" />
                    <DataGridTextColumn
                        Width="110"
                        Binding="{Binding DailyRate, StringFormat={}{0:C2}}"
                        Header="Daily Rate" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding CurrentOdometerKm, StringFormat={}{0:N0} km}"
                        Header="Odometer" />

                    <DataGridTemplateColumn Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Width="32"
                                    Height="32"
                                    Background="Transparent"
                                    Foreground="{DynamicResource PrimaryAccentBrush}"
                                    Style="{StaticResource TitleBarButtonStyle}"
                                    ToolTip="Edit Vehicle">
                                    <TextBlock
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="14"
                                        Text="&#xE70F;" />
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <Border
                            Margin="10,5,10,10"
                            Padding="15"
                            Background="{DynamicResource AppBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock
                                        Margin="0,0,0,2"
                                        FontSize="11"
                                        FontWeight="Bold"
                                        Foreground="{DynamicResource TextMutedBrush}"
                                        Text="VIN" />
                                    <TextBlock Foreground="{DynamicResource TextHighBrush}" Text="{Binding VIN}" />
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock
                                        Margin="0,0,0,2"
                                        FontSize="11"
                                        FontWeight="Bold"
                                        Foreground="{DynamicResource TextMutedBrush}"
                                        Text="Transmission" />
                                    <TextBlock Foreground="{DynamicResource TextHighBrush}" Text="{Binding Transmission}" />
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock
                                        Margin="0,0,0,2"
                                        FontSize="11"
                                        FontWeight="Bold"
                                        Foreground="{DynamicResource TextMutedBrush}"
                                        Text="Fuel Type" />
                                    <TextBlock Foreground="{DynamicResource TextHighBrush}" Text="{Binding FuelType}" />
                                </StackPanel>

                                <StackPanel Grid.Column="3">
                                    <TextBlock
                                        Margin="0,0,0,2"
                                        FontSize="11"
                                        FontWeight="Bold"
                                        Foreground="{DynamicResource TextMutedBrush}"
                                        Text="Description" />
                                    <TextBlock
                                        Foreground="{DynamicResource TextHighBrush}"
                                        Text="{Binding Description}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>

            </DataGrid>
        </Border>
    </Grid>
</UserControl>